spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/hyperboard}
    username: ${DATABASE_USER:postgres}
    password: ${DATABASE_PASSWORD:dev}
  flyway:
    enabled: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health
